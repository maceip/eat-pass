# Mobile coupled mint (Tier B)

Same eat-pass protocol as CVM demos; **weaker RA story** (device attestation, not silicon CVM).

| Platform | SDK | Policy example |
|----------|-----|----------------|
| Android | [`mobile/sdk-android/demo`](../../mobile/sdk-android/demo) | `policy/examples/mobile-android-example.json` |
| iOS | [`mobile/sdk-ios`](../../mobile/sdk-ios) | `policy/examples/mobile-ios-example.json` |

Label in talks: **same gate shape, different evidence collector**.

See [`docs/platforms.html`](../../docs/platforms.html) for gate + policy per surface.
